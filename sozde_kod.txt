FONKSİYON SecureCollatz(Seed, Uzunluk):
    x = Seed
    K = 0x55555555  (XOR maskesi - sabit)
    M = 2^64 - 59   (Büyük asal sayı - MOD için)
    
    DÖNGÜ Uzunluk kadar:
        EĞER x çift İSE:
            x = (x / 2) XOR K
        DEĞİLSE (x tek ise):
            x = (3 * x + 1) MOD M
        
        x = (x sağa 3 bit kaydır) OR (x sola 61 bit kaydır) // Bit Rotasyonu
        BİT = x'in en sağındaki bit (x MOD 2)
        ANAHTAR'a BİT'i ekle
    SON DÖNGÜ
    DÖNDÜR ANAHTAR